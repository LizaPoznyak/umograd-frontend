export interface UserResponse {
    id: number;
    username: string;
    email: string;
    roles: string[];
}

export interface ChildResponse {
    id: number;
    username: string;
    email: string;
}
